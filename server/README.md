IsokoHub server
===============

This small Express server provides a health endpoint for local development.

Setup
-----

```bash
cd server
npm install
npm start
```

The health check is available at `/api/health`.
